# Финальное задание на курсе Stepik по Selenium

**Для запуска всех тестов используйте <ins>pytest -v --tb=line --language=en</ins>**
**Для запуска тестов с пометкой need_review используйте <ins>pytest -v --tb=line --language=en -m need_review</ins>**